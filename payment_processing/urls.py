from django.urls import path
from . import views

app_name = 'payment_processing'

urlpatterns = [
    # Add payment processing URLs here if needed
    # For now, this is just a placeholder to prevent import errors
]
